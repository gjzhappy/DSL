          - patch_debug_json
          value_type: string
          variable: patch_debug_json
        - value_selector:
          - '1776000000019'
          - unapplied_refinements
          value_type: string
          variable: patch_result_json
        - value_selector:
          - '1776000000004'
          - query_refinement_json
          value_type: string
          variable: query_refinement_json
        - value_selector:
          - '1776000000024'
          - normalizer_valid
          value_type: boolean
          variable: normalizer_valid
        - value_selector:
          - '1776000000024'
          - normalizer_route
          value_type: string
          variable: normalizer_route
        - value_selector:
          - '1780000000009'
          - schema_runtime_context_json
          value_type: string
          variable: schema_runtime_context_json
      height: 80
      id: '1778000000001'
      position:
        x: 14540
        y: 120
      positionAbsolute:
        x: 14540
        y: 120
      selected: false
      sourcePosition: right
      targetPosition: left
      type: custom
      width: 242
    - data:
        cases:
        - case_id: valid
          conditions:
          - comparison_operator: is
            id: validator-route-valid
            value: valid
            varType: string
            variable_selector:
            - '1778000000001'
            - validator_route
          id: valid
          logical_operator: and
        - case_id: requires_clarification
          conditions:
          - comparison_operator: is
            id: validator-route-clarification
            value: requires_clarification
            varType: string
            variable_selector:
            - '1778000000001'
            - validator_route
          id: requires_clarification
          logical_operator: and
        - case_id: blocked
          conditions:
          - comparison_operator: is
            id: validator-route-blocked
            value: blocked
            varType: string
            variable_selector:
            - '1778000000001'
            - validator_route
          id: blocked
          logical_operator: and
        - case_id: needs_replan
          conditions:
          - comparison_operator: is
            id: validator-route-needs-replan
            value: needs_replan
            varType: string
            variable_selector:
            - '1778000000001'
            - validator_route
          id: needs_replan
          logical_operator: and
        desc: Phase 8 validator route：仅 valid 允许进入 Mongo 输入/编译执行，其他分支输出校验回答。
        selected: false
        title: 条件分支_validator结果判断
        type: if-else
      height: 246
      id: '1778000000002'
      position:
        x: 15000
        y: 120
      positionAbsolute:
        x: 15000
        y: 120
      selected: false
      sourcePosition: right
      targetPosition: left
      type: custom
      width: 242
    - data:
        code: |
          import json

          def _loads(value):
              if isinstance(value, dict):
                  return value
              if isinstance(value, str) and value.strip():
                  try:
                      parsed = json.loads(value)
                      return parsed if isinstance(parsed, dict) else {}
                  except Exception:
                      return {}
              return {}

          def _as_list(value):
              if isinstance(value, list):
                  return [str(x).strip() for x in value if str(x).strip()]
              if value in (None, '', {}, []):
                  return []
              return [str(value).strip()]

          def _first_stage(plan):
              stages = ((plan.get('execution_plan') or {}).get('stages') or plan.get('stages') or []) if isinstance(plan, dict) else []
              return stages[0] if isinstance(stages, list) and stages and isinstance(stages[0], dict) else {}

          def _add(out, value):
              text = str(value or '').strip()
              if text and text not in out:
                  out.append(text)

          def _hit_score(question, value, weight):
              text = str(value or '').strip()
              return weight if text and text.lower() in question.lower() else 0

          def _registry_match(question, registry):
              scores = []
              for item in registry.get('collections') or []:
                  if not isinstance(item, dict):
                      continue
                  name = str(item.get('collection_name') or '').strip()
                  if not name:
                      continue
                  score = 0; reasons = []
                  for key, weight in [('collection_name',100),('collection_label',90)]:
                      hit = _hit_score(question, item.get(key), weight)
                      if hit:
                          score += hit; reasons.append(key)
                  for key, weight in [('aliases',80),('primary_terms',60),('fields',45),('metrics',45)]:
                      hits = sum(1 for value in _as_list(item.get(key)) if _hit_score(question, value, weight))
                      if hits:
                          score += hits * weight; reasons.append(key)
                  desc_hits = 0
                  for token in str(item.get('description') or '').replace('，',' ').replace('。',' ').split():
                      if len(token) >= 2 and token in question:
                          desc_hits += 1
                  if desc_hits:
                      score += min(desc_hits, 3) * 10; reasons.append('description')
                  if score > 0:
                      scores.append({'collection_name': name, 'score': score, 'matched_fields': reasons})
              scores.sort(key=lambda x: x.get('score',0), reverse=True)
              if not scores:
                  return [], scores
              top = scores[0].get('score',0)
              winners = [x.get('collection_name') for x in scores if x.get('score',0) == top and x.get('collection_name')]
              return (winners if len(winners) == 1 else []), scores

          def _context_binding(qref, intent, old_plan):
              st = _first_stage(old_plan)
              base = str(old_plan.get('primary_collection') or st.get('collection') or st.get('primary_collection') or '').strip()
              return {'base_primary_collection': base, 'old_primary_collection': base, 'collection_switch': bool(qref.get('collection_switch'))}

          def main(question: str = '', query_refinement_json: str = '{}', normalized_turn_intent_json: str = '{}', last_context_json: str = '{}', old_normalized_semantic_plan_json: str = '{}', patch_reason: str = '', changed_fields: str = '', collection_registry_json: str = '', **kwargs):
              q = str(question or '').strip()
              qref = _loads(query_refinement_json)
              intent = _loads(normalized_turn_intent_json)
              old_plan = _loads(old_normalized_semantic_plan_json)
              registry = _loads(collection_registry_json)
              cb = _context_binding(qref, intent, old_plan)
              registry_names, scores = _registry_match(q, registry)
              old_primary = cb.get('base_primary_collection') or ''
              explicit_switch = bool(registry_names and old_primary and registry_names[0] != old_primary and qref.get('collection_switch'))
              selected = str(qref.get('selected_primary_collection') or qref.get('target_collection') or qref.get('collection') or '').strip()
              if explicit_switch:
                  names = registry_names; strategy = 'explicit_switch_by_registry'; collection_switch = True
              elif old_primary:
                  names = [old_primary]; strategy = 'inherit_previous'; collection_switch = False
              elif selected:
                  names = [selected]; strategy = 'selected_primary_collection'; collection_switch = False
              elif registry_names:
                  names = registry_names; strategy = 'registry_match'; collection_switch = False
              else:
                  names = []; strategy = 'registry_no_match'; collection_switch = False
              ready = bool(names); primary = names[0] if names else ''
              query = '\n'.join(['当前是 refine/full-replan。','metadata_type: mongo_schema','collection_name: '+primary,'只检索 candidate_collection_names 中 collection 对应的完整 schema metadata。','用户问题：'+q])
              debug = {'candidate_collection_scores': scores, 'strategy': strategy, 'context_binding': cb}
              return {
                  'query_text': query,
                  'old_normalized_semantic_plan_json': json.dumps(old_plan, ensure_ascii=False),
                  'query_refinement_json': json.dumps(qref, ensure_ascii=False),
                  'patch_reason': str(patch_reason or ''),
                  'changed_fields_json': json.dumps(_as_list(changed_fields), ensure_ascii=False),
                  'refine_retrieval_valid': ready,
                  'refine_retrieval_error': '' if ready else 'schema_missing',
                  'refine_query_debug_json': json.dumps(debug, ensure_ascii=False),
                  'context_binding_json': json.dumps(cb, ensure_ascii=False),
                  'must_include_collections_json': json.dumps(names, ensure_ascii=False),
                  'preferred_collections_json': json.dumps(names, ensure_ascii=False),
                  'deprecated_fields_json': '[]',
                  'collection_strategy': strategy,
                  'schema_strategy': 'metadata_exact' if ready else 'requires_clarification',
                  'base_primary_collection': old_primary,
                  'target_semantic_terms_json': json.dumps([q], ensure_ascii=False),
                  'candidate_collection_names': names,
                  'candidate_collection_name': primary,
                  'candidate_collection_ready': ready,
                  'candidate_collection_strategy': strategy,
                  'candidate_collection_scores_json': json.dumps(scores, ensure_ascii=False),
                  'candidate_collection_reason': 'matched by registry or inherited context' if ready else 'no collection available',
                  'collection_switch': collection_switch,
                  'requires_clarification': not ready,
                  'clarification_question': '' if ready else '请明确要查询的业务对象或 collection。',
              }
        code_language: python3
        desc: 为 refine full-replan 一次性 CandidateSchemas schema retrieval 构造查询，优先体现当前 refine
          语义。
        outputs:
          query_text:
            children:
            type: string
          old_normalized_semantic_plan_json:
            children:
            type: string
          query_refinement_json:
            children:
            type: string
          patch_reason:
            children:
            type: string
          changed_fields_json:
            children:
            type: string
          refine_retrieval_valid:
            children:
            type: boolean
          refine_retrieval_error:
            children:
            type: string
          refine_query_debug_json:
            children:
            type: string
          context_binding_json:
            children:
            type: string
          must_include_collections_json:
            children:
            type: string
          preferred_collections_json:
            children:
            type: string
          deprecated_fields_json:
            children:
            type: string
          collection_strategy:
            children:
            type: string
          schema_strategy:
            children:
            type: string
          base_primary_collection:
            children:
            type: string
          target_semantic_terms_json:
            children:
            type: string
          candidate_collection_names:
            type: array[string]
          candidate_collection_name:
            children:
            type: string
          candidate_collection_ready:
            children:
            type: boolean
          candidate_collection_strategy:
            children:
            type: string
          candidate_collection_scores_json:
            children:
            type: string
          candidate_collection_reason:
            children:
            type: string
          collection_switch:
            children:
            type: boolean
          requires_clarification:
            children:
            type: boolean
          clarification_question:
            children:
            type: string
        selected: false
        title: 代码执行_构建CandidateSchemas检索查询_Refine
        type: code
        variables:
        - value_selector:
          - sys
          - query
          value_type: string
          variable: question
        - value_selector:
          - '1776000000004'
          - query_refinement_json
          value_type: string
          variable: query_refinement_json
        - value_selector:
          - '1776000000004'
          - normalized_turn_intent_json
          value_type: string
          variable: normalized_turn_intent_json
        - value_selector:
          - '1776000000001'
          - last_context_json
          value_type: string
          variable: last_context_json
        - value_selector:
          - '1776000000001'
          - old_normalized_semantic_plan_json
          value_type: string
          variable: old_normalized_semantic_plan_json
        - value_selector:
          - '1776000000019'
          - patch_reason
          value_type: string
          variable: patch_reason
        - value_selector:
          - '1776000000019'
          - changed_fields
          value_type: string
          variable: changed_fields
        - value_selector:
          - env
          - COLLECTION_REGISTRY_JSON
          value_type: string
          variable: collection_registry_json
      height: 80
      id: '1776000000025'
      position:
        x: 5520
        y: 520
      positionAbsolute:
        x: 5520
        y: 520
      selected: false
      sourcePosition: right
      targetPosition: left
      type: custom
      width: 242
    - data:
        code: |
          import json
          import hashlib
          import re

          SCHEMA_CONTRACT_VERSION = 'schema_metadata_contract'
          ALIAS_INDEX_CONTRACT_VERSION = 'schema_alias_index_contract'
          CATALOG_VERSION = 'collection_catalog_contract'


          def _strip_quotes(v):
              v = str(v or '').strip()
              if len(v) >= 2 and v[0] == v[-1] and v[0] in {'"', "'"}:
                  return v[1:-1]
              return v


          def _split_inline_list(body):
              return [_strip_quotes(x.strip()) for x in re.split(r'\s*,\s*', str(body or '')) if x.strip()]


          def _parse_scalar(v):
              v = str(v or '').strip()
              if not v:
                  return ''
              if v in {'|', '>', '|-', '>-'}:
                  return ''
              if v in {'true', 'True'}:
                  return True
              if v in {'false', 'False'}:
                  return False
              if v in {'null', 'Null', 'NULL', '~'}:
                  return None
              if v.startswith('[') and v.endswith(']'):
                  return _split_inline_list(v[1:-1].strip())
              if v.startswith('{') and v.endswith('}'):
                  try:
                      return json.loads(v)
                  except Exception:
                      return _strip_quotes(v)
              if re.match(r'^-?\d+$', v):
                  try:
                      return int(v)
                  except Exception:
                      pass
              if re.match(r'^-?\d+\.\d+$', v):
                  try:
                      return float(v)
                  except Exception:
                      pass
              return _strip_quotes(v)


          def _parse_simple_yaml(text):
              root = {}
              stack = [(-1, root)]
              lines = str(text or '').replace('\r\n', '\n').replace('\r', '\n').split('\n')
              i = 0
              while i < len(lines):
                  raw = lines[i]
                  if not raw.strip() or raw.lstrip().startswith('#'):
                      i += 1
                      continue
                  indent = len(raw) - len(raw.lstrip(' '))
                  s = raw.strip()
                  mblock = re.match(r'^([A-Za-z_][\w\-]*)\s*:\s*([>|][\-+]?)\s*$', s)
                  if mblock:
                      key = mblock.group(1)
                      parts = []
                      base = None
                      j = i + 1
                      while j < len(lines):
                          nr = lines[j]
                          if nr.strip():
                              ni = len(nr) - len(nr.lstrip(' '))
                              if ni <= indent:
                                  break
                              if base is None:
                                  base = ni
                              parts.append(nr[base:] if len(nr) >= base else nr.strip())
                          else:
                              parts.append('')
                          j += 1
                      while stack and indent <= stack[-1][0]:
                          stack.pop()
                      parent = stack[-1][1]
                      if isinstance(parent, dict):
                          parent[key] = '\n'.join(parts).strip()
                      i = j
                      continue
                  while stack and indent <= stack[-1][0]:
                      stack.pop()
                  parent = stack[-1][1]
                  if s.startswith('- '):
                      item = s[2:].strip()
                      if not isinstance(parent, list):
                          i += 1
                          continue
                      if ':' in item and re.match(r'^[A-Za-z_][\w\-]*\s*:', item):
                          k, v = item.split(':', 1)
                          obj = {k.strip(): _parse_scalar(v)} if v.strip() else {k.strip(): {}}
                          parent.append(obj)
                          stack.append((indent, obj))
                          if not v.strip():
                              stack.append((indent + 2, obj[k.strip()]))
                      else:
                          parent.append(_parse_scalar(item))
                      i += 1
                      continue
                  if ':' in s:
                      k, v = s.split(':', 1)
                      key = k.strip()
                      val = v.strip()
                      if isinstance(parent, dict):
                          if val:
                              parent[key] = _parse_scalar(val)
                              i += 1
                              continue
                          j = i + 1
                          nxt = ''
                          while j < len(lines):
                              if lines[j].strip() and not lines[j].lstrip().startswith('#'):
                                  nxt = lines[j].strip()
                                  break
                              j += 1
                          container = [] if nxt.startswith('- ') else {}
                          parent[key] = container
                          stack.append((indent, container))
                  i += 1
              return root


          def _content(doc):
              if isinstance(doc, dict):
                  return str(doc.get('content') or doc.get('text') or doc.get('document') or '')
              return str(doc or '')


          def _extract_blocks(text):
              raw = str(text or '')
              blocks = []
              for pat in [
                  r'(^|\n)##\s*Schema Metadata\s*\n+```(?:yaml|yml|json)?\s*\n(.*?)\n```',
                  r'```(?:yaml|yml|json)?\s*\n(.*?(?:metadata_type\s*:\s*mongo_schema|"metadata_type"\s*:\s*"mongo_schema").*?)\n```',
              ]:
                  blocks.extend(m.group(2 if 'Schema Metadata' in pat else 1) for m in re.finditer(pat, raw, re.I | re.S))
              if not blocks and re.search(r'metadata_type\s*:\s*mongo_schema|"metadata_type"\s*:\s*"mongo_schema"', raw, re.I):
                  blocks.append(raw)
              return blocks


          def _parse_block(block):
              text = str(block or '').strip()
              if not text:
                  return {}
              if text.startswith('{'):
                  return json.loads(text)
              return _parse_simple_yaml(text)


          def _as_list(value):
              if value in (None, ''):
                  return []
              if isinstance(value, list):
                  return [x for x in value if x is not None and str(x).strip()]
              return [x for x in _split_inline_list(value) if str(x).strip()]


          def _str_list(value, limit=0):
              out, seen = [], set()
              for item in _as_list(value):
                  s = str(item or '').strip()
                  if s and s not in seen:
                      seen.add(s); out.append(s)
                      if limit and len(out) >= limit:
                          break
              return out


          def _named_map(value):
              result = {}
              if isinstance(value, dict):
                  for name, item in value.items():
                      if isinstance(item, dict):
                          obj = dict(item); obj.setdefault('name', name); result[str(obj.get('name') or name).strip()] = obj
              elif isinstance(value, list):
                  for item in value:
                      if isinstance(item, dict) and str(item.get('name') or '').strip():
                          result[str(item.get('name')).strip()] = dict(item)
              return result


          def _normalize_collection(doc):
              cname = str((doc or {}).get('collection_name') or '').strip()
              if not cname:
                  return None
              fields = _named_map(doc.get('fields'))
              metrics = _named_map(doc.get('metrics'))
              relations = doc.get('relations') if isinstance(doc.get('relations'), list) else []
              normalized = dict(doc)
              normalized.update({
                  'collection_name': cname,
                  'collection_label': str(doc.get('collection_label') or cname).strip() or cname,
                  'collection_aliases': _str_list(doc.get('collection_aliases')),
                  'fields': fields,
                  'metrics': metrics,
                  'relations': [r for r in relations if isinstance(r, dict)],
                  'query_rules': doc.get('query_rules') if isinstance(doc.get('query_rules'), dict) else {},
              })
              return normalized


          def _digest(obj, key):
              tmp = json.loads(json.dumps(obj or {}, ensure_ascii=False))
              if isinstance(tmp, dict):
                  tmp.pop(key, None)
              payload = json.dumps(tmp, ensure_ascii=False, sort_keys=True, separators=(',', ':'))
              return hashlib.sha256(payload.encode('utf-8')).hexdigest()


          def _add_alias(bucket, alias, collection, kind, canonical, label='', source='', field=''):
              alias = str(alias or '').strip()
              if not alias:
                  return
              bucket.setdefault(alias, [])
              item = {'collection': collection, 'kind': kind, 'canonical': canonical, 'label': str(label or '').strip(), 'source': source}
              if field:
                  item['field'] = field
              if item not in bucket[alias]:
                  bucket[alias].append(item)


          def _alias_index(meta):
              aliases, ca, fa, ma, va = {}, {}, {}, {}, {}
              simple = {'fields': {}, 'metrics': {}, 'collections': {}}
              for cname, coll in (meta.get('collections') or {}).items():
                  for alias, source in [(cname, 'collection.name'), (coll.get('collection_label'), 'collection.label')]:
                      _add_alias(aliases, alias, cname, 'collection', cname, coll.get('collection_label'), source)
                      _add_alias(ca, alias, cname, 'collection', cname, coll.get('collection_label'), source)
                      if alias: simple['collections'][str(alias)] = cname
                  for a in coll.get('collection_aliases') or []:
                      _add_alias(aliases, a, cname, 'collection', cname, coll.get('collection_label'), 'collection.aliases')
                      _add_alias(ca, a, cname, 'collection', cname, coll.get('collection_label'), 'collection.aliases')
                      simple['collections'][str(a)] = cname
                  for fname, f in (coll.get('fields') or {}).items():
                      for alias, source in [(fname, 'field.name'), (f.get('label'), 'field.label')] + [(a, 'field.aliases') for a in (f.get('aliases') or [])]:
                          _add_alias(aliases, alias, cname, 'field', fname, f.get('label'), source)
                          _add_alias(fa, alias, cname, 'field', fname, f.get('label'), source)
                          if alias: simple['fields'].setdefault(cname, {})[str(alias)] = fname
                      for val_alias, canonical_value in (f.get('value_aliases') or {}).items() if isinstance(f.get('value_aliases'), dict) else []:
                          _add_alias(aliases, val_alias, cname, 'value', canonical_value, val_alias, 'field.value_aliases', field=fname)
                          _add_alias(va, val_alias, cname, 'value', canonical_value, val_alias, 'field.value_aliases', field=fname)
                  for mname, m in (coll.get('metrics') or {}).items():
                      for alias, source in [(mname, 'metric.name'), (m.get('label'), 'metric.label')] + [(a, 'metric.aliases') for a in (m.get('aliases') or [])]:
                          _add_alias(aliases, alias, cname, 'metric', mname, m.get('label'), source)
                          _add_alias(ma, alias, cname, 'metric', mname, m.get('label'), source)
                          if alias: simple['metrics'].setdefault(cname, {})[str(alias)] = mname
              return {'contract_version': ALIAS_INDEX_CONTRACT_VERSION, 'aliases': aliases, 'collection_aliases': ca, 'field_aliases': fa, 'metric_aliases': ma, 'value_aliases': va, 'fields': simple['fields'], 'metrics': simple['metrics'], 'collections': simple['collections'], 'warnings': []}


          def _catalog(meta):
              cols = []
              warnings = []
              for idx, (cname, coll) in enumerate((meta.get('collections') or {}).items()):
                  metrics = []
                  for mname, m in (coll.get('metrics') or {}).items():
                      metrics.extend([mname, m.get('label')]); metrics.extend(m.get('aliases') or [])
                  dims = []
                  default_time = str(coll.get('default_time_field') or '').strip()
                  for fname, f in (coll.get('fields') or {}).items():
                      role = str(f.get('role') or f.get('semantic_type') or '').strip()
                      if role in {'dimension', 'time'} or f.get('groupable') or f.get('filterable') or (default_time and fname == default_time):
                          dims.extend([fname, f.get('label')]); dims.extend(f.get('aliases') or [])
                  related = []
                  for r in coll.get('relations') or []:
                      target = str((r or {}).get('target_collection') or '').strip()
                      if target and target not in related:
                          related.append(target)
                  try:
                      priority = int(coll.get('priority')) if str(coll.get('priority') or '').strip() != '' else 60
                  except Exception:
                      priority = 60
                  cols.append({'collection_name': cname, 'collection_label': str(coll.get('collection_label') or cname).strip() or cname, 'aliases': _str_list([cname, coll.get('collection_label')] + (coll.get('collection_aliases') or []), 32), 'description': str(coll.get('purpose') or coll.get('description') or '').strip(), 'default_time_field': default_time, 'primary_metrics': _str_list(metrics, 64), 'primary_dimensions': _str_list(dims, 80), 'supported_intents': _str_list(coll.get('supported_intents') or ['detail', 'aggregate_summary', 'trend', 'ranking', 'enrich'], 16), 'related_collections': related, 'priority': priority})
              if not cols:
                  warnings.append('schema metadata 中没有 collections，collection catalog 为空。')
              cat = {'catalog_version': CATALOG_VERSION, 'catalog_digest': '', 'collections': cols, 'warnings': warnings}
              cat['catalog_digest'] = _digest(cat, 'catalog_digest') if cols else ''
              return cat


          def _schema_context(meta):
              sections = []
              for cname, coll in (meta.get('collections') or {}).items():
                  sections.append('## collection=' + cname + '\n```json\n' + json.dumps(coll, ensure_ascii=False, indent=2) + '\n```')
              return '\n\n'.join(sections)


          def main(docs):
              warnings = []
              collections = {}
              schema_version = ''
              for doc in (docs or []):
                  text = _content(doc)
                  for block in _extract_blocks(text):
                      try:
                          raw = _parse_block(block)
                          if not isinstance(raw, dict):
                              warnings.append('跳过非对象 schema block')
                              continue
                          if raw.get('metadata_type') != 'mongo_schema' and not raw.get('collection_name'):
                              warnings.append('跳过非 mongo_schema block')
                              continue
                          if not schema_version:
                              schema_version = str(raw.get('schema_version') or raw.get('metadata_version') or '').strip()
                          coll = _normalize_collection(raw)
                          if coll:
                              collections[coll['collection_name']] = coll
                      except Exception as e:
                          warnings.append('CandidateSchemas block 解析失败: ' + str(e)[:160])
              meta = {'metadata_type': SCHEMA_CONTRACT_VERSION, 'contract_version': SCHEMA_CONTRACT_VERSION, 'schema_version': schema_version, 'schema_digest': '', 'collections': collections, 'warnings': warnings, 'parse_errors': []}
              meta['schema_digest'] = _digest(meta, 'schema_digest') if collections else ''
              alias = _alias_index(meta) if collections else {'contract_version': ALIAS_INDEX_CONTRACT_VERSION, 'aliases': {}, 'collection_aliases': {}, 'field_aliases': {}, 'metric_aliases': {}, 'value_aliases': {}, 'fields': {}, 'metrics': {}, 'collections': {}, 'warnings': []}
              cat = _catalog(meta)
              context_ref = {'collections': list(collections.keys()), 'schema_digest': meta.get('schema_digest', ''), 'schema_version': schema_version, 'catalog_digest': cat.get('catalog_digest', '')}
              if not docs:
                  warnings.append('CandidateSchemas retrieval returned no documents')
              if docs and not collections:
                  warnings.append('CandidateSchemas parse failed or contained no mongo_schema')
              meta['warnings'] = warnings
              cat['warnings'] = (cat.get('warnings') or []) + warnings
              return {
                  'schema_context': _schema_context(meta),
                  'schema_metadata_json': json.dumps(meta, ensure_ascii=False),
                  'schema_alias_index_json': json.dumps(alias, ensure_ascii=False),
                  'collection_catalog_json': json.dumps(cat, ensure_ascii=False),
                  'schema_context_ref_json': json.dumps(context_ref, ensure_ascii=False),
                  'candidate_schema_collections_json': json.dumps(list(collections.keys()), ensure_ascii=False),
                  'parse_warnings_json': json.dumps(warnings, ensure_ascii=False),
                  'catalog_parse_warnings_json': json.dumps(warnings, ensure_ascii=False),
              }
        code_language: python3
        desc: 解析 refine CandidateSchemas 检索结果中的 mongo_schema 文档，输出裁剪前 schema metadata、alias
          index、派生 catalog 与 schema context ref。
        outputs:
          schema_context:
            children:
            type: string
          schema_metadata_json:
            children:
            type: string
          schema_alias_index_json:
            children:
            type: string
          collection_catalog_json:
            children:
            type: string
          schema_context_ref_json:
            children:
            type: string
          candidate_schema_collections_json:
            children:
            type: string
          parse_warnings_json:
            children:
            type: string
          catalog_parse_warnings_json:
            children:
            type: string
        selected: false
        title: 代码执行_打包CandidateSchemas并派生Catalog_Refine
        type: code
        variables:
        - value_selector:
          - '1776000000032'
          - result
          value_type: array[object]
          variable: docs
      height: 80
      id: '1776000000026'
      position:
        x: 7640
        y: 520
      positionAbsolute:
        x: 7640
        y: 520
      selected: false
      sourcePosition: right
      targetPosition: left
      type: custom
      width: 242
    - data:
        code: "import json\nimport re\n\n# schema metadata C contract helpers\nimport
          copy\nimport hashlib\nimport json\nimport re\n\n\nCONTRACT_VERSION = 'schema_metadata_contract'\nALIAS_INDEX_CONTRACT_VERSION
          = 'schema_alias_index_contract'\nNON_SEMANTIC_SCHEMA_KEYS = {'warnings', 'parse_errors',
          'source_refs', 'raw_schema', 'raw_schema_text', 'schema_digest'}\n\n\ndef default_query_rules():\n
          \   return {\n        'require_time_range_for_aggregate': True,\n        'max_limit':
          100,\n        'allowed_operations': ['find', 'aggregate'],\n        'aggregate_required_when':
          [],\n        'find_allowed_when': [],\n        'sensitive_field_policy': 'deny_return_group_sort',\n
          \       'default_limit': 100,\n        'max_time_range_days': 366,\n    }\n\n\ndef
          _strip_quotes(v):\n    v = str(v or '').strip()\n    if len(v) >= 2 and v[0]
          == v[-1] and v[0] in {'\"', \"'\"}:\n        return v[1:-1]\n    return v\n\n\ndef
          _split_inline_list(body):\n    # Simple comma split that keeps quoted Chinese/ASCII
          aliases intact for the schema docs used here.\n    return [_strip_quotes(x.strip())
          for x in re.split(r'\\s*,\\s*', str(body or '')) if x.strip()]\n\n\ndef _parse_scalar(v):\n
          \   v = str(v or '').strip()\n    if not v:\n        return ''\n    if v in
          {'|', '>', '|-', '>-'}:\n        return ''\n    if v in {'true', 'True'}:\n
          \       return True\n    if v in {'false', 'False'}:\n        return False\n
          \   if v in {'null', 'Null', 'NULL', '~'}:\n        return None\n    if v.startswith('[')
          and v.endswith(']'):\n        return _split_inline_list(v[1:-1].strip())\n    if
          v.startswith('{') and v.endswith('}'):\n        try:\n            return json.loads(v)\n
          \       except Exception:\n            return _strip_quotes(v)\n    if re.match(r'^-?\\d+$',
          v):\n        try:\n            return int(v)\n        except Exception:\n            pass\n
          \   if re.match(r'^-?\\d+\\.\\d+$', v):\n        try:\n            return float(v)\n
          \       except Exception:\n            pass\n    return _strip_quotes(v)\n\n\ndef
          _parse_simple_yaml(text):\n    root = {}\n    stack = [(-1, root)]\n    lines
          = str(text or '').replace('\\r\\n', '\\n').replace('\\r', '\\n').split('\\n')\n
          \   i = 0\n    while i < len(lines):\n        raw = lines[i]\n        if not
          raw.strip() or raw.lstrip().startswith('#'):\n            i += 1\n            continue\n
          \       indent = len(raw) - len(raw.lstrip(' '))\n        s = raw.strip()\n
          \       mblock = re.match(r'^([A-Za-z_][\\w\\-]*)\\s*:\\s*([>|][\\-+]?)\\s*$',
          s)\n        if mblock:\n            key = mblock.group(1)\n            parts
          = []\n            base = None\n            j = i + 1\n            while j <
          len(lines):\n                nr = lines[j]\n                if nr.strip():\n
          \                   ni = len(nr) - len(nr.lstrip(' '))\n                    if
          ni <= indent:\n                        break\n                    if base is
          None:\n                        base = ni\n                    parts.append(nr[base:]
          if len(nr) >= base else nr.strip())\n                else:\n                    parts.append('')\n
          \               j += 1\n            while stack and indent <= stack[-1][0]:\n
          \               stack.pop()\n            parent = stack[-1][1]\n            if
          isinstance(parent, dict):\n                parent[key] = '\\n'.join(parts).strip()\n
          \           i = j\n            continue\n        while stack and indent <= stack[-1][0]:\n
          \           stack.pop()\n        parent = stack[-1][1]\n        if s.startswith('-
          '):\n            item = s[2:].strip()\n            if not isinstance(parent,
          list):\n                i += 1\n                continue\n            if ':'
          in item and re.match(r'^[A-Za-z_][\\w\\-]*\\s*:', item):\n                k,
          v = item.split(':', 1)\n                obj = {k.strip(): _parse_scalar(v)}
          if v.strip() else {k.strip(): {}}\n                parent.append(obj)\n                stack.append((indent,
          obj))\n                if not v.strip():\n                    stack.append((indent
          + 2, obj[k.strip()]))\n            else:\n                parent.append(_parse_scalar(item))\n
          \           i += 1\n            continue\n        if ':' in s:\n            k,
          v = s.split(':', 1)\n            key = k.strip()\n            val = v.strip()\n
          \           if isinstance(parent, dict):\n                if val:\n                    parent[key]
          = _parse_scalar(val)\n                    i += 1\n                    continue\n
          \               j = i + 1\n                nxt = ''\n                while j
          < len(lines):\n                    if lines[j].strip() and not lines[j].lstrip().startswith('#'):\n
          \                       nxt = lines[j].strip()\n                        break\n
          \                   j += 1\n                container = [] if nxt.startswith('-
          ') else {}\n                parent[key] = container\n                stack.append((indent,
          container))\n        i += 1\n    return root\n\n\ndef _json_loads_maybe(text):\n
          \   text = str(text or '').strip()\n    if not text:\n        raise ValueError('empty
          JSON text')\n    return json.loads(text)\n\n\ndef _parse_metadata_block(block):\n
          \   block = str(block or '').strip()\n    if not block:\n        return {}\n
          \   if block.startswith('{') or block.startswith('['):\n        return json.loads(block)\n
          \   return _parse_simple_yaml(block)\n\n\ndef extract_schema_metadata_blocks(text):\n
          \   raw = str(text or '')\n    blocks = []\n    # Prefer explicit Schema Metadata
          fenced blocks, YAML or JSON.\n    pattern = re.compile(r'(^|\\n)##\\s*Schema
          Metadata\\s*\\n+```(?:yaml|yml|json)?\\s*\\n(.*?)\\n```', re.I | re.S)\n    blocks.extend(m.group(2)
          for m in pattern.finditer(raw))\n    if blocks:\n        return blocks\n    #
          Then accept any fenced YAML/JSON block that declares mongo/schema contract metadata.\n
          \   pattern2 = re.compile(r'```(?:yaml|yml|json)?\\s*\\n(.*?(?:metadata_type\\s*:\\s*mongo_schema|\"metadata_type\"\\s*:\\s*\"mongo_schema\"|\"contract_version\"\\s*:\\s*\"schema_metadata_contract\").*?)\\n```',
          re.I | re.S)\n    blocks.extend(m.group(1) for m in pattern2.finditer(raw))\n
          \   return blocks\n\n\ndef _as_list(value):\n    if value is None or value ==
          '':\n        return []\n    if isinstance(value, list):\n        return [x for
          x in value if x is not None and str(x).strip()]\n    return [x for x in _split_inline_list(value)
          if str(x).strip()]\n\n\ndef _as_str_list(value):\n    return [str(x).strip()
          for x in _as_list(value) if str(x).strip()]\n\n\ndef _as_dict(value):\n    return
          value if isinstance(value, dict) else {}\n\n\ndef _normalize_named_map(value,
          kind, warnings, collection_name):\n    result = {}\n    if value is None or
          value == '':\n        return result\n    if isinstance(value, dict):\n        iterable
          = []\n        for name, item in value.items():\n            if isinstance(item,
          dict):\n                merged = dict(item)\n                merged.setdefault('name',
          name)\n                iterable.append(merged)\n            else:\n                warnings.append(f'{collection_name}.{kind}.{name}
          不是对象，已忽略。')\n        source_shape = 'dict'\n    elif isinstance(value, list):\n
          \       iterable = value\n        source_shape = 'list'\n    else:\n        warnings.append(f'{collection_name}.{kind}
          不是 list/dict，已按空对象处理。')\n        return result\n    for item in iterable:\n
          \       if not isinstance(item, dict):\n            warnings.append(f'{collection_name}.{kind}
          中存在非对象条目，已忽略。')\n            continue\n        name = str(item.get('name') or
          '').strip()\n        if not name:\n            warnings.append(f'{collection_name}.{kind}
          中存在缺少 name 的条目，已忽略。')\n            continue\n        normalized = copy.deepcopy(item)\n
          \       normalized['name'] = name\n        result[name] = normalized\n    return
          result\n\n\ndef _infer_function(expression):\n    expr = str(expression or '').strip().lower()\n
          \   m = re.match(r'^\\s*([a-z_][\\w]*)\\s*\\(', expr)\n    return m.group(1)
          if m else ''\n\n\ndef _normalize_field(field, collection_name, warnings):\n
          \   item = copy.deepcopy(field) if isinstance(field, dict) else {}\n    name
          = str(item.get('name') or '').strip()\n    sensitive = bool(item.get('sensitive',
          False))\n    pii = bool(item.get('pii', False))\n    role = str(item.get('role')
          or 'dimension').strip() or 'dimension'\n    if 'returnable' not in item:\n        item['returnable']
          = False if sensitive or pii else True\n        if not (sensitive or pii):\n
          \           warnings.append(f'{collection_name}.{name}.returnable 缺失，C-MVP 兼容期默认
          true。')\n    if 'projectable' not in item:\n        item['projectable'] = False
          if sensitive or pii else True\n        if not (sensitive or pii):\n            warnings.append(f'{collection_name}.{name}.projectable
          缺失，C-MVP 兼容期默认 true。')\n    normalized = {\n        'name': name,\n        'label':
          str(item.get('label') or name).strip(),\n        'type': str(item.get('type')
          or '').strip(),\n        'role': role,\n        'semantic_type': str(item.get('semantic_type')
          or role).strip() or role,\n        'description': str(item.get('description')
          or '').strip(),\n        'aliases': _as_str_list(item.get('aliases')),\n        'allowed_values':
          _as_str_list(item.get('allowed_values')),\n        'value_aliases': {str(k).strip():
          str(v).strip() for k, v in _as_dict(item.get('value_aliases')).items() if str(k).strip()
          and str(v).strip()},\n        'filterable': bool(item.get('filterable', False)),\n
          \       'groupable': False if (sensitive or pii) and 'groupable' not in item
          else bool(item.get('groupable', False)),\n        'sortable': False if (sensitive
          or pii) and 'sortable' not in item else bool(item.get('sortable', False)),\n
          \       'returnable': bool(item.get('returnable', False)),\n        'projectable':
          bool(item.get('projectable', False)),\n        'chartable': bool(item.get('chartable',
          False)),\n        'aggregatable': _as_str_list(item.get('aggregatable')),\n
          \       'sensitive': sensitive,\n        'pii': pii,\n        'pii_category':
          str(item.get('pii_category') or '').strip(),\n        'default_display': bool(item.get('default_display',
          False)),\n    }\n    # Preserve current/forward-compatible extra field properties.\n
          \   for k, v in item.items():\n        if k not in normalized and k not in NON_SEMANTIC_SCHEMA_KEYS:\n
          \           normalized[k] = v\n    return normalized\n\n\ndef _normalize_metric(metric,
          primary_key):\n    item = copy.deepcopy(metric) if isinstance(metric, dict)
          else {}\n    name = str(item.get('name') or '').strip()\n    function = str(item.get('function')
          or _infer_function(item.get('expression'))).strip()\n    field = str(item.get('field')
          or '').strip()\n    if not field and function == 'count' and primary_key:\n
          \       field = primary_key\n    source_fields = _as_str_list(item.get('source_fields'))\n
          \   if not source_fields and field:\n        source_fields = [field]\n    normalized
          = {\n        'name': name,\n        'label': str(item.get('label') or name).strip(),\n
          \       'role': str(item.get('role') or 'metric').strip() or 'metric',\n        'description':
          str(item.get('description') or '').strip(),\n        'aliases': _as_str_list(item.get('aliases')),\n
          \       'expression': str(item.get('expression') or '').strip(),\n        'function':
          function,\n        'field': field,\n        'source_fields': source_fields,\n
          \       'output_type': str(item.get('output_type') or 'number').strip() or 'number',\n
          \       'aggregate_only': bool(item.get('aggregate_only', True)),\n        'allowed_dimensions':
          _as_str_list(item.get('allowed_dimensions')),\n        'default_sort': str(item.get('default_sort')
          or 'desc').strip() or 'desc',\n        'chartable': bool(item.get('chartable',
          True)),\n    }\n    for k, v in item.items():\n        if k not in normalized
          and k not in NON_SEMANTIC_SCHEMA_KEYS:\n            normalized[k] = v\n    return
          normalized\n\n\ndef _normalize_relation(relation, collection_name, warnings):\n
          \   item = copy.deepcopy(relation) if isinstance(relation, dict) else {}\n    join_keys
          = item.get('join_keys') if isinstance(item.get('join_keys'), list) else []\n
          \   if not join_keys:\n        warnings.append(f'{collection_name}.relations
          关系缺少 join_keys。')\n    normalized_join_keys = []\n    for jk in join_keys:\n
          \       if isinstance(jk, dict):\n            normalized_join_keys.append({\n
          \               'source_field': str(jk.get('source_field') or '').strip(),\n
          \               'target_field': str(jk.get('target_field') or '').strip(),\n
          \               'match_type': str(jk.get('match_type') or 'exact').strip() or
          'exact',\n            })\n    normalized = {\n        'target_collection': str(item.get('target_collection')
