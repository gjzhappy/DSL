    - data:
        code: |
          import json

          def _loads(value):
              if isinstance(value, dict):
                  return value
              if isinstance(value, str) and value.strip():
                  try:
                      parsed = json.loads(value)
                      return parsed if isinstance(parsed, dict) else {}
                  except Exception:
                      return {}
              return {}

          def _list(value):
              if isinstance(value, list):
                  return [str(x).strip() for x in value if str(x).strip()]
              if value in (None, ''):
                  return []
              return [str(value).strip()]

          def _first_stage(plan):
              stages = ((plan.get('execution_plan') or {}).get('stages') or plan.get('stages') or []) if isinstance(plan, dict) else []
              return stages[0] if isinstance(stages, list) and stages and isinstance(stages[0], dict) else {}

          def _add(out, value):
              text = str(value or '').strip()
              if text and text not in out:
                  out.append(text)

          def _old_terms(last_context, old_plan):
              terms = []
              _add(terms, last_context.get('last_primary_collection'))
              for c in _list(last_context.get('last_related_collections')):
                  _add(terms, c)
              ref = last_context.get('schema_context_ref') if isinstance(last_context.get('schema_context_ref'), dict) else {}
              for c in _list(ref.get('collections')):
                  _add(terms, c)
              st = _first_stage(old_plan)
              _add(terms, old_plan.get('primary_collection') or st.get('collection') or st.get('primary_collection'))
              for f in _list(st.get('group_fields')):
                  _add(terms, f)
              _add(terms, st.get('metric_field'))
              _add(terms, st.get('metric_alias'))
              tr = st.get('time_range') if isinstance(st.get('time_range'), dict) else {}
              _add(terms, tr.get('field'))
              return terms

          def _refine_terms(query_refinement, turn_intent):
              terms = []
              for obj in (query_refinement, turn_intent):
                  if not isinstance(obj, dict):
                      continue
                  for key in ['refined_question', 'current_question', 'rewrite_question', 'user_correction', 'intent_summary', 'route_reason', 'patch_reason']:
                      _add(terms, obj.get(key))
                  for key in ['changed_fields', 'negated_fields', 'new_fields', 'group_fields', 'metrics', 'filters', 'business_terms', 'possible_new_concepts']:
                      for item in _list(obj.get(key)):
                          if isinstance(item, dict):
                              _add(terms, item.get('field') or item.get('name') or item.get('value'))
                          else:
                              _add(terms, item)
              return terms

          def main(question: str = '', query_refinement_json: str = '{}', normalized_turn_intent_json: str = '{}', last_context_json: str = '{}', old_normalized_semantic_plan_json: str = '{}', patch_reason: str = '', changed_fields: str = '', **kwargs):
              q = str(question or '').strip()
              qref = _loads(query_refinement_json)
              intent = _loads(normalized_turn_intent_json)
              last_context = _loads(last_context_json)
              old_plan = _loads(old_normalized_semantic_plan_json)
              current_terms = []
              _add(current_terms, q)
              for term in _refine_terms(qref, intent):
                  _add(current_terms, term)
              old_terms = _old_terms(last_context, old_plan)
              structural = any(token in (str(qref) + str(intent) + q).lower() for token in ['full_replan', 'structural', '不再', '改按', '换成', '重新', 'new chart'])
              query = '\n'.join([
                  '当前用户问题/refine：' + q,
                  '当前 refine 语义关键词（优先）：' + ' '.join(current_terms),
                  '上一轮 collection/field 弱参考（不得限制重新召回）：' + ' '.join(old_terms),
                  'query_refinement_json：' + json.dumps(qref, ensure_ascii=False),
                  'turn_intent_json：' + json.dumps(intent, ensure_ascii=False),
                  'patch_reason：' + str(patch_reason or ''),
                  'changed_fields：' + str(changed_fields or ''),
                  '是否结构性 full-replan：' + ('true' if structural else 'false'),
                  '请检索最相关的 Mongo schema 文档；必须优先体现当前 refine 语义，允许重新召回候选 schema。',
                  '需要 metadata_type: mongo_schema；包含 collection_name fields metrics relations query_rules aliases；用于 collection selection、group by、filters、metrics、relations。',
                  'Mongo schema metadata_type: mongo_schema fields metrics relations group by',
              ])
              debug = {'current_terms': current_terms, 'old_terms': old_terms, 'structural_full_replan': structural}
              return {
                  'query_text': query,
                  'old_normalized_semantic_plan_json': json.dumps(old_plan, ensure_ascii=False),
                  'query_refinement_json': json.dumps(qref, ensure_ascii=False),
                  'patch_reason': str(patch_reason or ''),
                  'changed_fields_json': json.dumps(_list(changed_fields), ensure_ascii=False),
                  'refine_retrieval_valid': True,
                  'refine_retrieval_error': '',
                  'refine_query_debug_json': json.dumps(debug, ensure_ascii=False),
              }
        code_language: python3
        desc: 为 refine full-replan 一次性 CandidateSchemas schema retrieval 构造查询，优先体现当前 refine
          语义。
        outputs:
          query_text:
            children:
            type: string
          old_normalized_semantic_plan_json:
            children:
            type: string
          query_refinement_json:
            children:
            type: string
          patch_reason:
            children:
            type: string
          changed_fields_json:
            children:
            type: string
          refine_retrieval_valid:
            children:
            type: boolean
          refine_retrieval_error:
            children:
            type: string
          refine_query_debug_json:
            children:
            type: string
        selected: false
        title: 代码执行_构建CandidateSchemas检索查询_Refine
        type: code
        variables:
        - value_selector:
          - sys
          - query
          value_type: string
          variable: question
        - value_selector:
          - '1776000000004'
          - query_refinement_json
          value_type: string
          variable: query_refinement_json
        - value_selector:
          - '1776000000004'
          - normalized_turn_intent_json
          value_type: string
          variable: normalized_turn_intent_json
        - value_selector:
          - '1776000000001'
          - last_context_json
          value_type: string
          variable: last_context_json
        - value_selector:
          - '1776000000001'
          - old_normalized_semantic_plan_json
          value_type: string
          variable: old_normalized_semantic_plan_json
        - value_selector:
          - '1776000000019'
          - patch_reason
          value_type: string
          variable: patch_reason
        - value_selector:
          - '1776000000019'
          - changed_fields
          value_type: string
          variable: changed_fields
      height: 80
      id: '1776000000025'
      position:
        x: 3000
        "y": 2200
      positionAbsolute:
        x: 3000
        "y": 2200
      selected: false
      sourcePosition: right
      targetPosition: left
      type: custom
      width: 242
